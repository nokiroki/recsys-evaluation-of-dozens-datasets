#! /bin/bash
source credentials

docker run \
 -d \
 -p ${CONTAINER_PORT}:8891 \
 --shm-size=8g \
 --memory=60g \
 --cpus=40 \
 --user ${DOCKER_USER_ID}:${DOCKER_GROUP_ID} \
 --name ${CONTAINER_NAME} \
 --rm -it --init \
 -v /home/n.belousov/${SRC}:/app \
 --gpus all \
 ${DOCKER_NAME} bash